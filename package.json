{
  "name": "websdk-sample-react-npm",
  "version": "1.8.5",
  "author": "Tommy Gaessler",
  "private": true,
  "homepage": "",
  "dependencies": {
    "@testing-library/jest-dom": "^5.11.4",
    "@testing-library/react": "^11.1.0",
    "@testing-library/user-event": "^12.1.10",
    "@zoomus/websdk": "^1.8.5",
    "express": "^4.17.1",
    "react-dom": "^16.8.6",
    "react-scripts": "4.0.1",
    "web-vitals": "^0.2.4"
  },
  "scripts": {
    "start": "rm -rf public/node_modules && mkdir public/node_modules && cp -R node_modules/@zoomus public/node_modules/@zoomus && react-scripts start",
    "build": "rm -rf docs && rm -rf public/node_modules && mkdir public/node_modules && cp -R node_modules/@zoomus public/node_modules/@zoomus && react-scripts build && mv build docs",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "resolutions": {
    "react": "16.8.6 "
  },
  "devDependencies": {
    "react": "^16.8.6"
  }
}
